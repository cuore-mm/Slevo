## 1. 共通ランナーの追加

- [ ] 1.1 `ui/common` 配下に `ImageMenuAction` 実行ランナーと依存受け渡しモデルを追加する
- [ ] 1.2 URL空判定、画像取得失敗、外部アプリ未解決時の失敗ハンドリングをランナー内で統一する

## 2. スレッド画面の置き換え

- [ ] 2.1 `ThreadScaffold` の `when(ImageMenuAction)` 実装を共通ランナー呼び出しへ置き換える
- [ ] 2.2 既存のメニュー閉じる処理、保存要求、NGダイアログ起動の画面固有フローを維持する

## 3. 画像ビューア画面の置き換え

- [ ] 3.1 `ImageViewerScreen` の `when(ImageMenuAction)` 実装を共通ランナー呼び出しへ置き換える
- [ ] 3.2 トップバー保存ボタンとその他メニュー双方から共通ランナー経由で実行されるようにする

## 4. 回帰確認

- [ ] 4.1 スレッド画面と画像ビューアで同一アクション（コピー/共有/外部起動/検索/保存）の結果一致を確認する
- [ ] 4.2 空URL時のガード、メニュークローズ、既存UI表示方式（BottomSheet/Dropdown）が維持されることを確認する
