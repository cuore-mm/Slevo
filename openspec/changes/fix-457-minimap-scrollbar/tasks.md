## 1. 仕様反映と算出ロジック差し替え

- [x] 1.1 `MomentumBar` の表示中インジケータ上端計算を `firstVisibleItemIndex` ベースの離散値から、スクロールオフセットを含む連続値計算へ変更する。
- [x] 1.2 表示中インジケータ高さ計算を `visibleItemsInfo.size` の整数近似から、先頭・末尾の部分可視量を反映した可視領域ベース計算へ変更する。
- [x] 1.3 0 件・1 件・極端な部分表示時に算出値が破綻しないよう、クランプと境界条件を実装して既存描画と共存させる。

## 2. 回帰確認と完了条件検証

- [ ] 2.1 ミニマップ有効時に微小スクロールでインジケータが段差なく連続移動することを確認する。
- [ ] 2.2 先頭・末尾が部分表示される位置でインジケータ高さが不自然にジャンプしないことを確認する。
- [ ] 2.3 勢いグラフ、URL マーカー、新着ライン、タップ/ドラッグジャンプ操作が従来どおり動作することを確認する。
