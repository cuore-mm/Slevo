## 1. スクロール保存方式の修正

- [ ] 1.1 `BbsRouteScaffold` のスクロール位置監視から、連続更新が停止するまで保存されない `debounce` 依存を取り除く
- [ ] 1.2 自動スクロール中も一定間隔で `firstVisibleItemIndex` と `firstVisibleItemScrollOffset` を保存する処理を追加する
- [ ] 1.3 同一スクロール位置の重複保存を抑制するため、保存前に位置変化を判定する
- [ ] 1.4 タブが非アクティブになるタイミングで現在の `LazyListState` を即時保存する
- [ ] 1.5 対象ページの Composable が破棄されるタイミングで現在の `LazyListState` を即時保存する

## 2. スレッド投稿行キーの安定化

- [ ] 2.1 `buildThreadListItemKey` を行インデックスに依存しない安定 key に変更する
- [ ] 2.2 レス番号に `dimmed`、`isAfter`、`depth` などの表示上の役割を組み合わせ、同一レス番号の複数表示でも key が重複しないことを確認する
- [ ] 2.3 表示位置が変わっても同じ表示役割の投稿行は同じ key になり、同じレス番号でも表示役割が異なる投稿行は別 key になることをテストで確認する

## 3. 回帰テストと確認

- [ ] 3.1 自動スクロール中にスワイプで隣のスレッドタブへ切り替え、戻ったときに離脱時点付近の位置で復元されることを確認する
- [ ] 3.2 自動スクロール中に他画面へ遷移し、戻ったときに離脱時点付近の位置で復元されることを確認する
- [ ] 3.3 手動スクロール後のタブ切り替えと復帰で、既存のスクロール復元挙動が維持されることを確認する
- [ ] 3.4 板画面など `BbsRouteScaffold` を共有する画面で、スクロール保存・復元の既存挙動が破壊されていないことを確認する
- [ ] 3.5 Android アプリの必須ビルドと単体テストを実行し、成功することを確認する

## 4. 仕上げ

- [ ] 4.1 実装差分に必要な KDoc・コメントがリポジトリ規約を満たしていることを確認する
- [ ] 4.2 Issue 490 の原因と修正内容を PR 説明に記載できる形で整理する
