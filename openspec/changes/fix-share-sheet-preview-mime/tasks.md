## 1. 再利用メタデータの拡張

- [x] 1.1 `ImageActionReuseRegistry.ReuseEntry` に拡張子とMIME情報を追加する
- [x] 1.2 登録時に拡張子/MIMEを正規化し、画像型でない値は既定値へ補正する

## 2. 共有再利用経路の修正

- [x] 2.1 `ImageCopyUtil` の再利用経路で、キャッシュ実ファイル名の拡張子依存を廃止する
- [x] 2.2 再利用メタデータの拡張子/MIMEを優先して共有ファイル生成と型判定を行う
- [x] 2.3 型情報欠損時は `jpg` / `image/jpeg` にフォールバックする

## 3. 表示成功時登録契約の統一

- [x] 3.1 スレッド画面側の画像表示成功登録で拡張子/MIMEを含めて登録する
- [x] 3.2 画像ビューア側の画像表示成功登録で拡張子/MIMEを含めて登録する

## 4. 検証

- [x] 4.1 スレッド画面起点の共有でシートに画像プレビューが表示されることを確認する
- [x] 4.2 画像ビューア起点の共有でシートに画像プレビューが表示されることを確認する
- [x] 4.3 共有機能と外部アプリ起動機能の既存成功動作が維持されることを確認する
- [ ] 4.4 CI（Android CI）を実行し、ビルドとテストが成功することを確認する
