## 1. 副作用責務の分離

- [ ] 1.1 `ThreadScreen` 内の最終既読更新ロジックを専用 effect 単位へ切り出し、既存発火条件を維持する
- [ ] 1.2 自動スクロール処理を専用 effect 単位へ切り出し、到達判定と `onAutoScrollBottom` 呼び出し条件を維持する
- [ ] 1.3 下端更新判定（overscroll、arming、drag 監視）を専用 effect/補助ロジックへ分離し、既存閾値と触覚条件を維持する

## 2. リスト描画と補助ロジックの分離

- [ ] 2.1 `LazyColumn` 構築と投稿行描画を責務別Composableへ分割し、`ThreadScreen` をオーケストレーター構成へ縮小する
- [ ] 2.2 ポップアップ基準座標算出ロジックを補助関数へ一元化し、投稿内イベントハンドラ重複を解消する
- [ ] 2.3 末尾スクロール先算出ロジックを補助関数化し、ジェスチャー下端移動処理で共通利用する

## 3. 挙動不変の確認

- [ ] 3.1 スクロール操作（通常スクロール、自動スクロール、下端更新）と既読更新が分割前と同一に動作することを確認する
- [ ] 3.2 投稿操作（メニュー、ポップアップ、画像遷移、返信導線）の結果が分割前と同一であることを確認する
- [ ] 3.3 ビルドとユニットテストを実行し、リファクタリングによる回帰がないことを確認する
