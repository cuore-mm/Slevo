## 1. 共通コンポーネント整理

- [x] 1.1 `FeedbackTooltipIconButton` を `ui/common` に移設し、既存ローカル実装を置換できる公開 API を定義する
- [x] 1.2 ツールチップ表示制御（表示可否フラグ変更時の dismiss を含む）と押下スケール挙動を共通部品に統合する

## 2. 対象画面への適用

- [x] 2.1 板画面・スレッド画面のボトムバー（`TabToolBar` 経由）にあるアイコンボタンを共通部品へ置換する
- [x] 2.2 検索バー内ボタン（戻る/クリア/音声入力）を共通部品へ置換し、既存検索挙動を維持する
- [x] 2.3 設定画面トップバー内ボタンを共通部品へ置換し、既存遷移・メニュー表示契約を維持する

## 3. ジェスチャー設定メニュー統一

- [x] 3.1 ジェスチャー設定画面の `DropdownMenu` を `AnchoredOverlayMenu` に置換し、アンカー座標取得を導入する
- [x] 3.2 メニュー外タップ/戻る操作での dismiss 契約と既存メニュー項目動作の維持を確認する

## 4. 検証

- [x] 4.1 対象画面でツールチップ表示、押下フィードバック、メニュー表示位置が統一されていることを確認する
- [x] 4.2 ビルドとユニットテストを実行し、既存機能が破壊されていないことを確認する
