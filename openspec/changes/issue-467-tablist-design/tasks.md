## 1. タブ一覧レイアウト再設計

- [ ] 1.1 `OpenBoardsList` をカードベースUIへ変更し、上段にサービス名・板名・件数情報・閉じる操作を配置する
- [ ] 1.2 `OpenThreadsList` をカードベースUIへ変更し、上段にサービス名・板名・レス数・閉じる操作を配置する
- [ ] 1.3 スレッドカードの新着レス表示を板画面と同等のバッジスタイルに統一する

## 2. 下部2段操作群の導入

- [ ] 2.1 `TabsPagerContent` のページ切替導線を上部 `TabRow` から下部上段の切替UIへ移行する
- [ ] 2.2 `TabScreenContent` の FAB を廃止し、下部下段にタブ作成・更新の独立アイコンボタンを実装する
- [ ] 2.3 現在ページが板タブ時は更新ボタンを非表示にする表示条件を実装する

## 3. 更新導線とスクロール終端の整備

- [ ] 3.1 `OpenThreadsList` からプル更新機能を削除し、更新処理を下部更新ボタン経由に一本化する
- [ ] 3.2 一覧末尾が下部2段操作群に重ならないよう、リスト下部余白（content padding）を追加する

## 4. 検証

- [ ] 4.1 板/スレ切替、タブ作成、更新、タブクローズ、タブ遷移の既存契約が維持されることを確認する
- [ ] 4.2 ビルドとユニットテストを実行し、回帰がないことを確認する
