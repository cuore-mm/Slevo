## 1. 下端プルアップ判定の再設計

- [ ] 1.1 `ThreadScreen` の下端更新判定を「最下部で静止後に開始したドラッグのみ有効」へ変更する
- [ ] 1.2 慣性スクロール延長での誤発火を防ぐ状態遷移（アーム/ディスアーム）を実装する

## 2. フィードバックとインジケーター統一

- [ ] 2.1 閾値未到達→到達の遷移ごとに `GestureThresholdActivate` 触覚を発火する
- [ ] 2.2 矢印アイコン表示を廃止し、プル中/更新中とも `ContainedLoadingIndicator` 系表示に統一する
- [ ] 2.3 下部インジケーター表示位置をボトムバー高さ連動で調整し、下部UIとの重なりを回避する

## 3. 動作検証

- [ ] 3.1 慣性スクロール延長で更新が発火しないことを確認する
- [ ] 3.2 最下部静止後のプルアップでのみ更新が発火することを確認する
- [ ] 3.3 閾値未到達→到達のたびに `GestureThresholdActivate` 触覚が発生することを確認する
