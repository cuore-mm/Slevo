## 1. 投稿サムネイルの失敗表示固定

- [ ] 1.1 `ImageThumbnailGrid` で失敗状態時は画像リクエストを発行しない描画分岐へ変更する
- [ ] 1.2 失敗サムネイルのスクロール再表示でローディングを経由せず失敗アイコンを維持することを確認する
- [ ] 1.3 失敗アイコンタップ時のみ再取得を開始することを確認する

## 2. ビューア本体の失敗表示固定

- [ ] 2.1 `ImageViewerPager` で失敗ページは自動再取得せず失敗オーバーレイ表示を維持するよう変更する
- [ ] 2.2 ページ離脱・復帰時にローディング再表示が発生しないことを確認する
- [ ] 2.3 本体リロードボタンタップ時のみ再取得が開始されることを確認する

## 3. 下部サムネイルバーの失敗表示固定

- [ ] 3.1 `ImageViewerThumbnailBar` で失敗項目は自動再取得せず失敗アイコン表示を維持するよう変更する
- [ ] 3.2 スクロール復帰時にローディングへ戻らないことを確認する
- [ ] 3.3 失敗表示項目タップ時も既存のページ切り替え契約を維持することを確認する

## 4. 回帰確認

- [ ] 4.1 issue #449 で追加した3箇所の再読み込み導線が明示操作で動作することを確認する
- [ ] 4.2 自動再取得抑止後も ImageViewerTopBar に変更が入っていないことを確認する
