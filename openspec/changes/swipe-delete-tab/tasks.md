## 1. ページ切替スワイプの廃止

- [x] 1.1 `TabsPagerContent` の `HorizontalPager` でユーザー操作による横スクロールを無効化し、下部 switch からのページ切替は維持する。
- [x] 1.2 板/スレ切替時の `onPageChanged`、選択状態、下部操作群の表示が既存どおり動作することを確認する。

## 2. タブカードのスワイプ削除 UI

- [x] 2.1 `TabListCard` にスワイプ削除可否と削除確定コールバックを追加し、未固定かつ通常時のみ横スワイプ gesture を受け付ける。
- [x] 2.2 左右どちらのスワイプでも削除しきい値を超えた場合に削除確定し、しきい値未満ではカードを通常位置へ戻す表示を実装する。
- [x] 2.3 スワイプ中の視覚表現を追加し、既存のカード表示、固定アイコン、閉じるボタン、長押し選択時の透明化・スケール表現を維持する。
- [x] 2.4 `combinedClickable` のタップ・長押しと横スワイプ、`LazyColumn` の縦スクロールが意図せず競合しないよう gesture 処理を調整する。

## 3. 一覧側との接続

- [x] 3.1 `TabScreenContent` から `TabsPagerContent`、`OpenBoardsList`、`OpenThreadsList` へ `uiState.isInLongPressSelectionMode` を渡す。
- [x] 3.2 `OpenBoardsList` で未固定板タブのスワイプ削除確定時に既存の `requestRemove()` を呼び、削除アニメーションと `closeBoardTab` 経路へ合流させる。
- [x] 3.3 `OpenThreadsList` で未固定スレッドタブのスワイプ削除確定時に既存の `requestRemove()` を呼び、削除アニメーションと `closeThreadTab` 経路へ合流させる。
- [x] 3.4 削除アニメーション中のカードではタップ、長押し、閉じる、スワイプ削除を再実行しないことを維持する。

## 4. 固定タブ・通常時以外の制御

- [x] 4.1 固定済みの板タブとスレッドタブではスワイプ削除が実行されないことを実装・確認する。
- [x] 4.2 長押し選択中は板タブとスレッドタブのスワイプ削除が実行されないことを実装・確認する。
- [x] 4.3 固定済みタブの固定アイコン表示と閉じるボタン非表示の既存挙動を維持する。

## 5. テストと検証

- [ ] 5.1 タブ一覧のページ横スワイプがページ切替を発生させず、下部 switch では切り替わることを確認するテストまたは手動確認手順を追加する。
- [ ] 5.2 未固定の板タブ・スレッドタブを左右スワイプで削除できることを確認するテストまたは手動確認手順を追加する。
- [ ] 5.3 固定済みタブと長押し選択中のタブがスワイプ削除されないことを確認するテストまたは手動確認手順を追加する。
- [ ] 5.4 `./gradlew :app:assembleDebug` と `./gradlew :app:testDebugUnitTest` を実行し、成功を確認する。
