## 1. ページ切替スワイプの廃止

- [x] 1.1 `TabsPagerContent` の `HorizontalPager` でユーザー操作による横スクロールを無効化し、下部 switch からのページ切替は維持する。
- [x] 1.2 板/スレ切替時の `onPageChanged`、選択状態、下部操作群の表示が既存どおり動作することを確認する。

## 2. タブカードのスワイプ削除 UI

- [x] 2.1 `TabListCard` にスワイプ削除可否と削除確定コールバックを追加し、未固定かつ通常時のみ横スワイプ gesture を受け付ける。
- [x] 2.2 左右どちらのスワイプでも削除しきい値を超えた場合に削除確定し、しきい値未満ではカードを通常位置へ戻す表示を実装する。
- [x] 2.3 スワイプ中の視覚表現を追加し、既存のカード表示、固定アイコン、閉じるボタン、長押し選択時の透明化・スケール表現を維持する。
- [x] 2.4 `combinedClickable` のタップ・長押しと横スワイプ、`LazyColumn` の縦スクロールが意図せず競合しないよう gesture 処理を調整する。

## 3. 一覧側との接続

- [x] 3.1 `TabScreenContent` から `TabsPagerContent`、`OpenBoardsList`、`OpenThreadsList` へ `uiState.isInLongPressSelectionMode` を渡す。
- [x] 3.2 `OpenBoardsList` で未固定板タブのスワイプ削除確定時に既存の `requestRemove()` を呼び、削除アニメーションと `closeBoardTab` 経路へ合流させる。
- [x] 3.3 `OpenThreadsList` で未固定スレッドタブのスワイプ削除確定時に既存の `requestRemove()` を呼び、削除アニメーションと `closeThreadTab` 経路へ合流させる。
- [x] 3.4 削除アニメーション中のカードではタップ、長押し、閉じる、スワイプ削除を再実行しないことを維持する。

## 4. 固定タブ・通常時以外の制御

- [x] 4.1 固定済みの板タブとスレッドタブではスワイプ削除が実行されないことを実装・確認する。
- [x] 4.2 長押し選択中は板タブとスレッドタブのスワイプ削除が実行されないことを実装・確認する。
- [x] 4.3 固定済みタブの固定アイコン表示と閉じるボタン非表示の既存挙動を維持する。

## 5. テストと検証

- [x] 5.1 タブ一覧のページ横スワイプがページ切替を発生させず、下部 switch では切り替わることを確認するテストまたは手動確認手順を追加する。
- [x] 5.2 未固定の板タブ・スレッドタブを左右スワイプで削除できることを確認するテストまたは手動確認手順を追加する。
- [x] 5.3 固定済みタブと長押し選択中のタブがスワイプ削除されないことを確認するテストまたは手動確認手順を追加する。
- [x] 5.4 `./gradlew :app:assembleDebug` と `./gradlew :app:testDebugUnitTest` を実行し、成功を確認する。

## 6. スワイプ挙動の洗練（カード幅上限・専用アニメーション・右から左のみ）

- [x] 6.1 `TabListCard` でカード幅を `onGloballyPositioned` で測定し、スワイプ移動量上限をカード幅とする。削除しきい値はカード幅の 40% とする。
- [x] 6.2 スワイプ削除を右から左方向のみ有効とし、`offsetX` は `coerceIn(-cardWidthPx, 0f)` で負の値のみ許可する。左から右のスワイプは削除対象外とする。
- [x] 6.3 スワイプ削除確定時に、`offsetX.animateTo(-cardWidthPx)` による専用退出アニメーションを実装する。アニメーション完了後に `onSwipeDelete` を呼び出し、既存の削除フローへ合流させる。
- [x] 6.4 ボタン削除および長押しメニュー削除時は従来の `animateItem` 退出アニメーションを維持し、スワイプ削除時は専用アニメーションで置き換える。
- [x] 6.5 退出アニメーション中（`isFlyingOut`）のカードでタップ、長押し、閉じるボタン、スワイプ削除を再実行しないようにする。
- [x] 6.6 CI でビルド・テストをパスする。

## 7. 速度を考慮したスワイプ削除判定

- [x] 7.1 `TabListCard` の gesture 処理を `VelocityTracker` で速度取得できる pointer input 構成へ更新する。
- [x] 7.2 距離判定をカード幅の 35% とし、右から左方向の移動距離がしきい値を超えた場合に削除確定する。
- [x] 7.3 速度判定を 700dp/s 相当以上の左方向速度、かつ 20dp 相当以上の左方向移動距離として実装する。
- [x] 7.4 距離判定または速度判定のいずれかを満たした場合のみ、既存の左方向専用退出アニメーションを開始する。
- [x] 7.5 左から右方向、固定タブ、長押し選択中、退出アニメーション中では速度判定でも削除されないことを維持する。
- [x] 7.6 CI でビルド・テストをパスする。

## 8. スワイプと縦スクロールの競合を DragMode で解決する

- [x] 8.1 `TabListCard` の gesture 処理を `detectHorizontalDragGestures` から自前の方向判定へ置き換え、`DragMode` enum（Undecided / HorizontalSwipe / VerticalScroll）を導入する。
- [x] 8.2 未確定状態（Undecided）では touch slop を超えた移動量で方向を判定し、横移動が優勢な場合に `HorizontalSwipe` を確定する。
- [x] 8.3 `HorizontalSwipe` に確定したら以降の pointer イベントを consume し、`LazyColumn` への縦スクロール伝播を阻止する。
- [x] 8.4 `VerticalScroll` に確定した場合は `offsetX` を 0 にアニメーションで戻し、縦スクロールは `LazyColumn` へ任せる。
- [x] 8.5 速度判定・距離判定・専用退出アニメーション・固定タブ/長押し選択中/退出アニメーション中の無効化は既存仕様を維持する。
- [x] 8.6 CI でビルド・テストをパスする。

## 9. スワイプジェスチャーの安定化（入力面と描画面の分離）

- [x] 9.1 `TabListCard` の `pointerInput` を `offset` を適用する `Card` から、移動しない外側 `Box` へ移動する。
- [x] 9.2 方向判定の `totalDx` / `totalDy` と横移動 `dx` の計算を `change.position` の絶対座標差分から、`positionChange()` の累積差分へ置き換える。
- [x] 9.3 `offsetX` の更新を加算型ではなく `totalDx.coerceIn(-cardWidthPx, 0f)` の直接算出に統一し、右方向移動の混入を防ぐ。
- [x] 9.4 `VelocityTracker` への入力をアニメーション済み offset ではなく、差分累積で構成した追跡座標へ変更する。
- [x] 9.5 タップ/長押しの誤発火を防ぐため、`combinedClickable` の有効条件に「カードが通常位置（offsetX == 0）」を追加する。
- [ ] 9.6 CI でビルド・テストをパスする。
