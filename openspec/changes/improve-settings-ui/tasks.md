## 1. テーマ設定モデルと保存層

- [ ] 1.1 `ThemeMode` 相当の 3 状態モデル（ライト、ダーク、システムテーマに従う）を追加し、表示名・保存値・テーマ適用値を扱えるようにする
- [ ] 1.2 `SettingsLocalDataSource` と `SettingsRepository` に 3 状態テーマ設定の observe / set API を追加し、既存 boolean API の利用箇所を移行する
- [ ] 1.3 DataStore に新しいテーマ設定保存 key を追加し、未保存時はシステムテーマ、既存 `dark_mode` がある場合はライト/ダークへ互換変換する
- [ ] 1.4 アプリのテーマ適用箇所で `ThemeMode` から `darkTheme` を導出し、システム追従時は OS テーマ変更に追従する

## 2. 全般設定 UI

- [ ] 2.1 `SettingsUiState` を 3 状態テーマ設定を保持する形へ更新し、ViewModel で保存値の監視と選択更新を扱う
- [ ] 2.2 `SettingsGeneralScreen` のダークテーマスイッチを `SettingsCardWithListItems` のテーマ設定項目へ置き換え、現在選択中のテーマ名を表示する
- [ ] 2.3 テーマ設定項目のアンカー bounds と expanded state を管理し、`AnchoredOverlayMenu` で 3 つの選択肢を表示する
- [ ] 2.4 テーマ選択肢の押下で設定を保存し、メニューを閉じる動作を実装する
- [ ] 2.5 全般設定画面の Preview を追加または更新し、3 状態テーマ設定の表示を確認できるようにする

## 3. スレッド設定 UI

- [ ] 3.1 `SettingsThreadScreen` の並び順設定とスクロールバー表示設定を `SettingsCardWithListItems` ベースのカード内リスト項目へ置き換える
- [ ] 3.2 デフォルト並び順項目に現在選択中の「番号順」または「ツリー順」を表示する
- [ ] 3.3 デフォルト並び順項目のアンカー bounds と expanded state を管理し、`AnchoredOverlayMenu` で「番号順」「ツリー順」を表示する
- [ ] 3.4 並び順選択肢の押下で既存の並び順保存 API を呼び出し、メニューを閉じる動作を実装する
- [ ] 3.5 ミニマップ付きスクロールバー表示設定の保存・反映契約を維持したまま、カード内 switch 項目として表示する
- [ ] 3.6 スレッド設定画面の Preview を追加または更新し、カード表示と選択項目表示を確認できるようにする

## 4. 検証とテスト

- [ ] 4.1 テーマ設定の既定値、既存 boolean 設定の互換読み取り、3 状態保存の単体テストを追加または更新する
- [ ] 4.2 ViewModel のテーマ選択更新とスレッド並び順更新が UiState と Repository 呼び出しへ反映されることを確認する
- [ ] 4.3 Compose UI または手動確認で、テーマ選択メニューと並び順選択メニューが `AnchoredOverlayMenu` として開閉し、外側タップ・戻るで閉じることを確認する
- [ ] 4.4 `./gradlew test` とビルド確認を実施し、変更後も単体テストとビルドが成功することを確認する
