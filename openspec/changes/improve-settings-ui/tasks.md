## 1. テーマ設定モデルと保存層

- [x] 1.1 `ThemeMode` 相当の 3 状態モデル（ライト、ダーク、システムテーマに従う）を追加し、表示名・保存値・テーマ適用値を扱えるようにする
- [x] 1.2 `SettingsLocalDataSource` と `SettingsRepository` に 3 状態テーマ設定の observe / set API を追加し、既存 boolean API と `dark_mode` key を削除する
- [x] 1.3 DataStore に新しいテーマ設定保存 key を追加し、未保存時はシステムテーマとして扱う。既存 `dark_mode` の互換変換は実装しない
- [x] 1.4 アプリのテーマ適用箇所で `ThemeMode` から `darkTheme` を導出し、システム追従時は OS テーマ変更に追従する

## 2. 汎用アンカー選択メニュー

- [x] 2.1 `AnchoredOverlayMenu` を用いた汎用選択メニューを追加し、選択肢リスト、現在値、選択時コールバック、dismiss コールバックを受け取れるようにする
- [x] 2.2 汎用選択メニュー内の選択済み項目を primary color、太字、右端チェックアイコンで表示する
- [x] 2.3 汎用選択メニューの Preview を追加し、未選択項目と選択済み項目の表示差分を確認できるようにする

## 3. 全般設定 UI

- [x] 3.1 `SettingsUiState` を 3 状態テーマ設定を保持する形へ更新し、ViewModel で保存値の監視と選択更新を扱う
- [x] 3.2 `SettingsGeneralScreen` のダークテーマスイッチを `SettingsCardWithListItems` のテーマ設定項目へ置き換え、現在選択中のテーマ名を supporting text に表示する
- [x] 3.3 テーマ設定項目のアンカー bounds と expanded state を管理し、汎用選択メニューで 3 つの選択肢を表示する
- [x] 3.4 テーマ選択肢の押下で設定を保存し、メニューを閉じる動作を実装する
- [x] 3.5 全般設定画面の Preview を追加または更新し、3 状態テーマ設定の表示を確認できるようにする

## 4. スレッド設定 UI

- [x] 4.1 `SettingsThreadScreen` の並び順設定とスクロールバー表示設定を `SettingsCardWithListItems` ベースのカード内リスト項目へ置き換える
- [x] 4.2 デフォルト並び順項目に現在選択中の「番号順」または「ツリー順」を supporting text で表示する
- [x] 4.3 デフォルト並び順項目のアンカー bounds と expanded state を管理し、汎用選択メニューで「番号順」「ツリー順」を表示する
- [x] 4.4 並び順選択肢の押下で既存の並び順保存 API を呼び出し、メニューを閉じる動作を実装する
- [x] 4.5 ミニマップ付きスクロールバー表示設定の保存・反映契約を維持したまま、カード内 switch 項目として表示する
- [x] 4.6 スレッド設定画面の Preview を追加または更新し、カード表示と選択項目表示を確認できるようにする

## 5. 検証とテスト

- [x] 5.1 テーマ設定の既定値、3 状態保存、既存 boolean 設定を参照しないことの単体テストを追加または更新する
- [x] 5.2 ViewModel のテーマ選択更新とスレッド並び順更新が UiState と Repository 呼び出しへ反映されることを確認する
- [x] 5.3 Compose UI または手動確認で、テーマ選択メニューと並び順選択メニューが汎用選択メニューとして開閉し、外側タップ・戻るで閉じることを確認する
- [x] 5.4 Compose UI または手動確認で、選択済み項目が primary color、太字、右端チェックアイコンで表示されることを確認する
- [x] 5.5 `./gradlew test` とビルド確認を実施し、変更後も単体テストとビルドが成功することを確認する
