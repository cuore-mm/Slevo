## 1. 共通保存フロー基盤の作成

- [ ] 1.1 画像保存の正規化・権限判定・pending URL 管理・保存結果判定を扱う共通コーディネータを `ui/common` 配下に追加する
- [ ] 1.2 単体保存/一括保存の結果通知文言を共通フォーマッタへ集約し、既存文言リソースとの互換性を維持する

## 2. ViewModel 契約の統一

- [ ] 2.1 スレッド画面 ViewModel を、共通コーディネータ利用の保存イベント契約（保存要求・権限結果受理・UIイベント発火）へ移行する
- [ ] 2.2 画像ビューア ViewModel に同一の保存イベント契約を実装し、Composable ローカルの pending URL 管理を廃止する

## 3. 画面側実装の置き換え

- [ ] 3.1 `ThreadScaffold` の保存処理を ViewModel API 呼び出し中心へ置換し、権限ランチャー連携のみ UI 層に残す
- [ ] 3.2 `ImageViewerScreen` の保存処理を ViewModel API 呼び出し中心へ置換し、保存関連の重複ロジックを削除する
- [ ] 3.3 `ImageMenuActionRunner` 経由の単体保存/一括保存が両画面で同一フローを通るよう接続を確認する

## 4. 検証

- [ ] 4.1 スレッド画面と画像ビューアで、単体保存・一括保存・権限拒否・権限許可後再開の挙動が一致することを確認する
- [ ] 4.2 画像保存完了時のトースト文言が両画面で同一条件で表示されることを確認する
- [ ] 4.3 ビルドとユニットテストを実行し、回帰がないことを確認する
