## 1. 判定要件の実装

- [x] 1.1 `detectDirectionalGesture` の終了・キャンセル経路を見直し、方向未確定（None）では無効通知を発火しないように修正する
- [x] 1.2 無効通知の発火条件を、水平開始後の矛盾軌跡（複数相反方向の同時成立）に限定する

## 2. 画面挙動の整合

- [x] 2.1 Board/Thread 画面で `onGestureInvalid` の表示挙動が新要件どおりになることを確認し、必要な調整を行う
- [x] 2.2 有効方向ジェスチャー（単一方向・複合方向）のアクション実行が既存どおり維持されることを確認する

## 3. 検証と回帰確認

- [x] 3.1 通常の縦スクロール操作で「無効なジェスチャーです」が表示されないことを再現手順で確認する
- [x] 3.2 無効軌跡入力時のみ無効通知が1回発火し、方向アクションが実行されないことを確認する
