## 1. 実装

- [x] 1.1 `BbsRouteScaffold.kt` の `LaunchedEffect(isActive, tab)` を `LaunchedEffect(isActive, tabKey)` に変更する
- [x] 1.2 変更箇所のコメントを更新する（必要に応じて）

## 2. 検証

- [x] 2.1 ビルドが成功することを確認する
- [x] 2.2 Android CI でビルドとテストが成功することを確認する
