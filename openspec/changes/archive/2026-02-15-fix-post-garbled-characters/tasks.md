## 1. 文字変換ユーティリティ整備

- [x] 1.1 Shift_JIS 非対応文字を NCR へ変換する共通ヘルパーを実装し、複数コードポイント絵文字を扱えることを確認する
- [x] 1.2 Shift_JIS で表現可能な文字を変更しないケースのテスト観点を整理する

## 2. 投稿送信経路への適用

- [x] 2.1 `PostRemoteDataSourceImpl` の一次送信でフォーム構築を `add` ベースへ統一し、値に共通ヘルパーを適用する
- [x] 2.2 `PostRemoteDataSourceImpl` の確認送信で hidden parameter を含む全値に同一変換と同一エンコード経路を適用する
- [x] 2.3 `ThreadCreateRemoteDataSourceImpl` の一次送信/確認送信でも同じ変換・エンコード経路を適用する

## 3. 検証

- [x] 3.1 絵文字・結合絵文字を含む投稿で文字化けが発生しないことを確認する
- [x] 3.2 返信投稿とスレ立て投稿の双方で、`MESSAGE`/`name`/`mail`/`subject` に対して同一変換が適用されることを確認する
- [x] 3.3 返信投稿とスレ立て投稿の双方で既存の投稿成功フローが維持されることを確認する
