## 1. タブ一覧レイアウト再設計

- [x] 1.1 `OpenBoardsList` をカードベースUIへ変更し、上段にサービス名・板名・件数情報・閉じる操作を配置する
- [x] 1.2 `OpenThreadsList` をカードベースUIへ変更し、上段にサービス名・板名・レス数・閉じる操作を配置する
- [x] 1.3 スレッドカードの新着レス表示を板画面と同等のバッジスタイルに統一する

## 2. 下部1段操作群の導入

- [x] 2.1 `TabsPagerContent` のページ切替導線を上部 `TabRow` から下部中央の切替UIへ移行する
- [x] 2.2 `TabScreenContent` の FAB を廃止し、下部1段に作成・切替・更新の横並び操作群を実装する
- [x] 2.3 現在ページが板タブ時は右スロットをプレースホルダー化して更新ボタンを非表示にする

## 3. 更新導線とスクロール終端の整備

- [x] 3.1 `OpenThreadsList` からプル更新機能を削除し、更新処理を下部更新ボタン経由に一本化する
- [x] 3.2 一覧末尾が下部2段操作群に重ならないよう、リスト下部余白（content padding）を追加する

## 4. 検証

- [x] 4.1 板/スレ切替、タブ作成、更新、タブクローズ、タブ遷移の既存契約が維持されることを確認する
- [x] 4.2 ビルドとユニットテストを実行し、回帰がないことを確認する


## 5. タブ削除アニメーション

- [x] 5.1 `OpenBoardsList` のカード削除を退出アニメーション付きに変更し、完了後に削除処理を呼び出す
- [x] 5.2 `OpenThreadsList` のカード削除を退出アニメーション付きに変更し、完了後に削除処理を呼び出す
- [x] 5.3 退出中カードの二重タップで遷移/削除が再実行されないことを確認する
