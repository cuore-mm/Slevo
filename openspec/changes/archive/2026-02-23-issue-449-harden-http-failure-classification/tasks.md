## 1. 失敗分類ロジックの厳密化

- [x] 1.1 `ImageLoadFailureType` の HTTP ステータス抽出をメッセージ regex 依存から型ベース判定へ置換する
- [x] 1.2 404/410 判定を HTTP 例外のステータスコード由来に限定し、その他は `UNKNOWN` へフォールバックする
- [x] 1.3 cause chain 探索の上限と判定順序を明文化し、既存呼び出し契約を壊さないことを確認する

## 2. UI 契約の回帰確認

- [x] 2.1 投稿サムネイルで HTTP 404/410 のみ専用表示になり、非 HTTP 失敗はリロード導線が出ることを確認する
- [x] 2.2 画像ビューア本体で HTTP 404/410 専用表示と `UNKNOWN` 時の再試行導線が維持されることを確認する
- [x] 2.3 下部サムネイルバーで HTTP 404/410 数字表示のみが適用され、非 HTTP 失敗で誤適用されないことを確認する

## 3. 検証と仕上げ

- [x] 3.1 例外メッセージに `404/410` を含む非 HTTP 失敗ケースで `UNKNOWN` になることを確認する
- [x] 3.2 issue #449 系変更（再試行契約・TopBar 非変更）との整合が崩れていないことを確認する
