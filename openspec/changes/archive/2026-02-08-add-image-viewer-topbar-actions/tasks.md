## 1. トップバーアクション追加

- [x] 1.1 `ImageViewerTopBar` に保存ボタンとその他ボタンの表示・クリックハンドラを追加する
- [x] 1.2 画像ビューア画面で保存ボタン押下時に現在表示中画像の単体保存を実行する

## 2. その他メニュー制御

- [x] 2.1 `ImageViewerScreen` の `DropdownMenu` 開閉状態を `UiState` で管理し、その他ボタン押下で開閉する
- [x] 2.2 バー非表示へ遷移した際に `DropdownMenu` を閉じる同期処理を追加する
- [x] 2.3 その他メニューに単体保存以外の既存画像アクションを表示し、選択時に処理を実行する

## 3. 表示確認

- [x] 3.1 トップバー表示時に保存/その他ボタンが表示されることを確認する
- [x] 3.2 その他ボタン押下でメニュー表示、バー非表示時にメニューが閉じることを確認する
- [x] 3.3 保存ボタンで単体保存、その他メニューで単体保存以外の既存画像アクションが動作することを確認する
