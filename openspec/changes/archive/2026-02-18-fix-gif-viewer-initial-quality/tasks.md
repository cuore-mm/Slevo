## 1. 原因固定化ポイントの解消

- [x] 1.1 `ImageViewerPager` の画像リクエスト生成を見直し、初回表示で低解像度条件が固定化されない構成へ変更する
- [x] 1.2 GIF サムネイル起点でビューアを開いた初回ページが、ページ移動なしで高品質表示されることを確認する

## 2. 既存契約の回帰防止

- [x] 2.1 表示成功時の再利用メタデータ登録（`diskCacheKey` など）を維持し、共有/外部起動前処理の契約を保持する
- [x] 2.2 進捗表示、shared transition、保存（単体/一括）の既存挙動に回帰がないことを確認する

## 3. 検証と完了条件

- [x] 3.1 GIF を含む複数画像投稿で「初回のみ粗い→往復で改善」が再現しないことを再確認する
- [x] 3.2 変更内容と確認結果を記録し、実装タスク完了条件を満たした状態で `tasks.md` のチェックを更新する
