## 1. 差分計算の整理

- [x] 1.1 `ThreadSummaryDao` に、対象板の全既存スレッド ID を取得する用途と未アーカイブのスレッド ID を取得する用途を分けた DAO メソッドを定義する
- [x] 1.2 `BoardRepository.refreshThreadList` で、存在判定には全既存 ID、アーカイブ対象抽出には未アーカイブ ID を使うように処理を分離する
- [x] 1.3 subject.txt に再出現したアーカイブ済みスレッドが、既存行更新により `isArchived = false` へ戻ることを確認する

## 2. アーカイブ更新の安全化

- [x] 2.1 アーカイブ対象 ID の更新を SQLite 変数上限より十分小さいチャンク単位に分割する
- [x] 2.2 チャンクサイズを定数化し、`boardId` 分の SQL 変数も考慮した値にする
- [x] 2.3 分割更新が既存の板更新トランザクション内で実行され、途中失敗時に部分確定しないことを確認する

## 3. テスト

- [x] 3.1 大量のアーカイブ済み過去スレッドが存在する板更新で、過去スレッドが再アーカイブ対象にならず更新が成功する単体テストを追加する
- [x] 3.2 大量の未アーカイブ削除対象が存在する板更新で、チャンク分割により全対象がアーカイブされる単体テストを追加する
- [x] 3.3 アーカイブ済みスレッドが subject.txt に再出現した場合に、既存行としてタイトル・レス数・順位が更新され未アーカイブ化される単体テストを追加する
- [x] 3.4 通常の subject.txt 更新、新規スレッド追加、レス数更新の既存挙動が維持されることを確認する

## 4. 検証

- [x] 4.1 `too many SQL variables` を再現する規模の ID 数を想定して、DAO 呼び出しが単一の巨大 `IN` 句にならないことを確認する
- [x] 4.2 CI のユニットテストを実行し、追加・既存テストが成功することを確認する
- [x] 4.3 変更範囲のコメントがリポジトリのコメント規約を満たしていることを確認する
