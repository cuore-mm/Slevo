## 1. ボトムバー挙動ロジックの単純化

- [x] 1.1 `BottomBarUtils` から最下部到達の sticky 判定（末尾可視判定・保持タイマー・閾値パラメータ）を削除する
- [x] 1.2 ボトムバー挙動を `exitAlways` ベースのスクロール方向制御に統一し、`canScroll` をスクロール可否条件のみに整理する

## 2. 板画面/スレッド画面への適用確認

- [x] 2.1 `BoardScaffold` で簡素化後の共通ボトムバー挙動を適用し、既存のツールバー構成を維持する
- [x] 2.2 `ThreadScaffold` でも同一挙動を適用し、ポップアップ表示時のスクロール抑止条件との整合を確認する

## 3. 仕様ベースの回帰確認

- [x] 3.1 板画面・スレッド画面で初期表示時にツールバー全表示となることを確認する
- [x] 3.2 下方向スクロールでボタン群のみ隠れ、上方向スクロールで全表示に戻ることを確認する
- [x] 3.3 最下部到達時に自動再表示が発生しないことを確認する
