## 1. 状態管理と検索ロジック

- [x] 1.1 `TabsUiState` に検索モード状態と検索クエリを追加し、既存の単一収集方針を維持する
- [x] 1.2 `TabsViewModel` に検索開始、検索終了、検索クエリ更新の操作を追加し、検索終了時にクエリをクリアする
- [x] 1.3 タブ検索用の共通マッチング関数を追加し、既存 `toHiragana()` と `ignoreCase = true` を再利用して大小文字とかな表記の差を吸収する
- [x] 1.4 板タブ検索フィルタ関数を追加し、板名と表示サービス名を対象に部分一致で絞り込む
- [x] 1.5 スレッドタブ検索フィルタ関数を追加し、板名とスレ名を対象に部分一致で絞り込む

## 2. 検索入力 UI の共通化

- [x] 2.1 `SearchBottomBar` の検索入力、閉じる、消去、音声入力、IME Search 処理を共通 Composable へ切り出す
- [x] 2.2 既存 `SearchBottomBar` を共通入力 Composable のラッパーとして再構成し、既存 API と表示挙動を維持する
- [x] 2.3 タブ一覧上部検索バー用 Composable を追加し、共通入力 Composable と Haze 背景を組み合わせる
- [x] 2.4 タブ一覧右上検索ボタン用 Composable を追加し、通常表示時に Haze 背景上で検索開始操作を提供する

## 3. タブ一覧 UI 統合

- [x] 3.1 `TabScreenContent` で検索状態を受け取り、通常時は右上検索ボタン、検索モード時は上部検索バーを表示する
- [x] 3.2 `TabScreenContent` で検索クエリに基づく板タブ一覧・スレッドタブ一覧を導出し、`TabsPagerContent` へ渡す
- [x] 3.3 `TabListBottomControls` に検索モード引数を追加し、検索中は作成・板/スレ切替・更新/キャンセル操作群を非表示にする
- [x] 3.4 検索モード中も下部更新進捗インジケーター領域を維持し、更新中は進捗を表示する
- [x] 3.5 検索開始時に長押し選択状態を解除し、検索 UI と長押しメニューが同時表示されないようにする

## 4. スクロール位置保存と戻る操作

- [x] 4.1 `RemovableTabList` に外部 `LazyListState` を渡せる引数を追加し、既存呼び出し元のデフォルト挙動を維持する
- [x] 4.2 `OpenBoardsList` と `OpenThreadsList` に `LazyListState` 引数を追加し、`TabsPagerContent` 経由で上位から状態を渡す
- [x] 4.3 `TabScreenContent` で板一覧・スレッド一覧それぞれのスクロール状態を保持し、検索モード開始時点ではスクロール位置を保存しないようにする
- [x] 4.4 検索クエリが空から非空へ切り替わる直前に、完全リスト上の板一覧・スレッド一覧の位置を保存する
- [x] 4.5 検索モード中の `BackHandler` を追加し、戻る操作で検索終了とスクロール復元を実行する
- [x] 4.6 検索クエリクリアまたは検索結果表示中の検索終了で完全リストへ戻った後、保存したスクロール位置へ復元する
- [x] 4.7 検索結果表示が発生していない検索終了ではスクロール復元を行わず、保存位置が範囲外の場合は有効範囲へ丸める

## 5. テストと確認

- [x] 5.1 タブ検索フィルタのユニットテストを追加し、空クエリ、板名一致、スレ名一致、大小文字無視、ひらがな/カタカナ無視を検証する
- [x] 5.2 検索入力共通化後も既存 `SearchBottomBar` の閉じる、消去、音声入力、IME Search の契約が維持されることを確認する
- [x] 5.3 タブ一覧で検索ボタン、上部検索バー、下部操作群非表示、進捗インジケーター維持の表示状態を確認する
- [x] 5.4 戻る操作で検索が終了し、検索前のスクロール位置へ復元されることを確認する
- [x] 5.5 Android アプリのビルドとユニットテストを実行し、成功を確認する

## 6. アニメーション追加

- [x] 6.1 `TabListTopSearchArea` の検索ボタンと検索バーの表示切替に `AnimatedVisibility` を適用し、フェード＋上方向スライドの入出アニメーションを提供する
- [x] 6.2 `TabListBottomControls` の下部操作群表示/非表示切替に `AnimatedVisibility` を適用し、フェード＋下方向スライド＋縦展開/収縮の入出アニメーションを提供する
- [x] 6.3 アニメーション適用後に Android アプリのビルドとユニットテストを実行し、成功を確認する

## 7. 上部 Haze タップガード

- [x] 7.1 `TabListTopSearchArea` の上部 Haze 領域全体で背面リストへのタップ透過を防ぎ、検索ボタンや検索バーの子要素操作は維持する
- [x] 7.2 上部 Haze タップガードとスクロール保存/復元仕様の修正後に Android アプリのビルドとユニットテストを実行し、成功を確認する
