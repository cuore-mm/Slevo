## 1. システムバー復元ロジック修正

- [x] 1.1 `ImageViewerScreen` の `DisposableEffect(activity)` で、開始時の system bars 可視状態を取得・保持する
- [x] 1.2 `onDispose` で既存の外観復元に加え、保持した可視状態に応じて `show/hide(systemBars)` を実行する

## 2. 回帰確認

- [x] 2.1 画像ビューアでバーを非表示にして戻ったとき、遷移先画面でシステムバーが表示状態へ復元されることを確認する
- [x] 2.2 既存のバー表示トグル・アイコン外観制御・スクラム表示が破綻しないことを確認する
- [x] 2.3 ビルドおよびユニットテストを実行し、回帰がないことを確認する
