## 1. ポップアップ一覧のスクロールバー対応

- [x] 1.1 `ReplyPopup` の投稿一覧コンテナを `Column + verticalScroll` から `LazyColumn` 構成へ置き換える
- [x] 1.2 投稿表示順・区切り線・既存タップイベント伝播（返信番号/返信元/ID/URL/画像）の挙動を維持したまま描画を移行する
- [x] 1.3 `LazyColumnScrollbar` を適用し、ポップアップ内でスクロール位置インジケーターが表示されるようにする

## 2. 非ドラッグ設定と動作確認

- [x] 2.1 `ScrollbarSettings` に `selectionMode = ScrollbarSelectionMode.Disabled` を設定し、つまみドラッグを無効化する
- [x] 2.2 スクロール可否判定を実装し、表示領域に収まるポップアップではスクロールバーを非表示にする
- [x] 2.3 長文ポップアップでスクロールバー表示と位置追従が動作し、短文ポップアップではスクロールバーが表示されないことを確認する
- [x] 2.4 スクロールバー表示中/非表示中のいずれでも、既存のポップアップ内操作（返信遷移・ID遷移・URL/画像操作）が維持されることを確認する
