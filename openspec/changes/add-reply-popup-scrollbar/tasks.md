## 1. ポップアップ一覧のスクロールバー対応

- [ ] 1.1 `ReplyPopup` の投稿一覧コンテナを `Column + verticalScroll` から `LazyColumn` 構成へ置き換える
- [ ] 1.2 投稿表示順・区切り線・既存タップイベント伝播（返信番号/返信元/ID/URL/画像）の挙動を維持したまま描画を移行する
- [ ] 1.3 `LazyColumnScrollbar` を適用し、ポップアップ内でスクロール位置インジケーターが表示されるようにする

## 2. 非ドラッグ設定と動作確認

- [ ] 2.1 `ScrollbarSettings` に `selectionMode = ScrollbarSelectionMode.Disabled` を設定し、つまみドラッグを無効化する
- [ ] 2.2 長文ポップアップでスクロールバー表示と位置追従が動作することを確認する
- [ ] 2.3 スクロールバー表示中でも既存のポップアップ内操作（返信遷移・ID遷移・URL/画像操作）が維持されることを確認する

## 3. スコープ明確化

- [ ] 3.1 「スクロール不可時はスクロールバー非表示」は本変更の対象外として、必要に応じて後続Issueへ切り出す
