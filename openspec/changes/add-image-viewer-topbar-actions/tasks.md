## 1. トップバーアクション追加

- [ ] 1.1 `ImageViewerTopBar` に保存ボタンとその他ボタンの表示・クリックハンドラを追加する
- [ ] 1.2 画像ビューア画面で保存ボタン押下イベントを現在表示中画像に紐づけて処理へ委譲する

## 2. その他メニュー制御

- [ ] 2.1 `ImageViewerScreen` に `DropdownMenu` の開閉状態を追加し、その他ボタン押下で開閉する
- [ ] 2.2 バー非表示へ遷移した際に `DropdownMenu` を閉じる同期処理を追加する

## 3. 表示確認

- [ ] 3.1 トップバー表示時に保存/その他ボタンが表示されることを確認する
- [ ] 3.2 その他ボタン押下でメニュー表示、バー非表示時にメニューが閉じることを確認する
