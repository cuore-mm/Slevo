## 1. 進捗取得基盤の準備

- [x] 1.1 Coil/OkHttp 経路で画像ダウンロード進捗（0.0..1.0）を通知できる共通コンポーネントを追加する
- [x] 1.2 総量不明・キャッシュヒット時に無段階表示へフォールバックする進捗状態契約を定義する

## 2. サムネイル進捗表示の実装（先行）

- [x] 2.1 `ImageThumbnailGrid` で URL ごとの進捗状態を購読し、進捗可視化インジケータを表示する
- [x] 2.2 サムネイルの既存遷移可否制御（表示成功時のみ遷移）と進捗表示が競合しないことを確認する

## 3. 画像ビューア画面の進捗表示実装

- [x] 3.1 `ImageViewerPager` の主画像読み込み中に進捗インジケータを表示する
- [x] 3.2 `ImageViewerThumbnailBar` の各サムネイル項目で進捗状態を表示する
- [x] 3.3 主画像と下部サムネイルの成功/失敗終端でインジケータを終了し、既存のタップ・スワイプ・バー表示切替契約を維持する

## 4. 非回帰確認

- [x] 4.1 shared transition、メニュー操作、画像切替の既存契約が変わらないことを確認する
- [ ] 4.2 ビルドとユニットテストを実行し、失敗時は修正して再実行する
