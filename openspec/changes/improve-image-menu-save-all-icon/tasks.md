## 1. メニューアイコン表示の実装

- [ ] 1.1 `ImageMenu` の「レス内の画像をすべて保存」項目に、download をベースとしたアイコン表示スロットを追加する
- [ ] 1.2 右上に `filter_none` バッジを重ねる合成アイコンComposableを実装し、BottomSheet の既存サイズ内で視認できる比率に調整する
- [ ] 1.3 既存の単体保存項目および他メニュー項目の表示契約を維持し、合成アイコン適用対象を「すべて保存」項目のみに限定する

## 2. 検証

- [ ] 2.1 複数画像を含むレスで画像メニューを開き、「レス内の画像をすべて保存」に合成アイコンが表示されることを確認する
- [ ] 2.2 同一メニュー内の「この画像を保存」で複数対象バッジが表示されないことを確認する
- [ ] 2.3 既存の一括保存実行条件（複数画像時のみ表示）と保存動作が変化していないことを確認する
