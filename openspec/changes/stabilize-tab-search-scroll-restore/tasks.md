## 1. 状態モデル設計

- [x] 1.1 板一覧・スレッド一覧それぞれの index/offset を保持する検索スクロールスナップショット型を定義する
- [x] 1.2 検索解除時の復元と検索クエリ変更時の先頭表示を表す一回限りのスクロール命令型を定義する
- [x] 1.3 `TabListUiState` に UI が実行すべき未消費スクロール命令を追加する

## 2. ViewModelへの検索復元状態集約

- [x] 2.1 `TabListViewModel` に検索前スクロールスナップショットと前回検索クエリ相当の状態を追加する
- [x] 2.2 検索クエリが空から非空へ変わる直前に、UI から受け取ったスクロールスナップショットを保存するメソッドを追加する
- [x] 2.3 検索クエリ更新時に旧クエリと新クエリを比較し、必要なスクロール命令を発行する
- [x] 2.4 検索解除時に検索モード・検索クエリをクリアし、保存済みスナップショットがある場合は復元命令を発行する
- [x] 2.5 スクロール命令実行後に UI から呼び出す consume メソッドを追加し、同じ命令の再実行を防ぐ
- [x] 2.6 検索状態を完全に破棄するメソッドを追加し、検索モード・検索クエリ・復元スナップショット・未消費命令をまとめてクリアできるようにする

## 3. TabScreenContentの副作用整理

- [x] 3.1 `TabScreenContent` から検索復元用の `remember` 状態と前回クエリ保持を削除する
- [x] 3.2 検索クエリ入力時、空から非空へ変わる直前に現在の `LazyListState` index/offset を ViewModel へ渡す
- [x] 3.3 ViewModel のスクロール命令を `LaunchedEffect` で監視し、復元命令では完全リスト上の有効範囲へ丸めて `scrollToItem` を実行する
- [x] 3.4 検索クエリ変更による先頭表示命令では、現在表示中ページのリストだけ `scrollToItem(0)` を実行する
- [x] 3.5 スクロール命令を実行した後、ViewModel の consume メソッドを呼んで命令を消費する

## 4. BottomSheetの検索状態リセット

- [x] 4.1 `TabsBottomSheet` が dismiss 時に検索状態破棄を呼び出せるよう、必要なコールバックまたは ViewModel 操作を整理する
- [x] 4.2 `BbsRouteScaffold` のタブ一覧 BottomSheet dismiss 処理で検索状態を完全に破棄してからシートを閉じる
- [ ] 4.3 BottomSheet 再表示時に検索モードではない初期状態で表示されることを確認する

## 5. テスト

- [x] 5.1 `TabListViewModel` の検索クエリ空→非空でスクロールスナップショット保存が維持されることをユニットテストする
- [x] 5.2 `TabListViewModel` の非空→空で復元命令が発行され、consume 後に再発行されないことをユニットテストする
- [x] 5.3 検索クエリ変更時に現在表示中ページだけの先頭表示命令が発行されることをユニットテストする
- [x] 5.4 検索状態完全破棄で検索モード・検索クエリ・復元スナップショット・未消費命令がすべてクリアされることをユニットテストする
- [ ] 5.5 可能であれば Compose UI テストで、検索中の再Composition相当後に検索解除してスクロール位置が復元されることを検証する

## 6. 検証

- [ ] 6.1 起動直後の通常タブ一覧で検索解除時のスクロール位置復元が維持されることを確認する
- [ ] 6.2 他画面からタブ一覧へ戻った後、検索解除時にスクロール位置が復元されることを確認する
- [ ] 6.3 スレ画面または板画面から開くタブ一覧 BottomSheet で検索解除時にスクロール位置が復元されることを確認する
- [ ] 6.4 タブ一覧 BottomSheet を検索中に閉じて再表示したとき、検索なしの状態で開くことを確認する
- [x] 6.5 Android CI のビルドとユニットテストを実行し、成功を確認する

## 7. 安定性優先の追加修正

- [ ] 7.1 検索解除時に復元スクロール命令を即時発行せず、復元待ちスナップショットとして保持する状態設計へ更新する
- [ ] 7.2 UI が `searchQuery == ""` かつ完全リストを描画対象にした後に、復元待ちスナップショットを使って `scrollToItem` を実行する
- [ ] 7.3 復元実行後に復元待ち状態を consume し、再Compositionだけでは同じ復元を再実行しないようにする
- [ ] 7.4 検索クエリ変更時の先頭表示も、クエリ更新後の検索結果リストが描画対象になった後に現在ページだけへ適用する
- [ ] 7.5 BottomSheet dismiss の `resetSearchState()` が復元待ち状態・未消費命令を安全に破棄し、通常の検索解除復元とは競合しないことを確認する
- [ ] 7.6 タイミング競合確認用のログまたはテスト観点を用意し、復元実行時の `searchQuery`、表示リストサイズ、対象 index/offset、consume タイミングを確認できるようにする
- [ ] 7.7 安定化後に ViewModel/Compose UI の回帰テストを追加し、検索解除・クエリ変更・再Composition・BottomSheet 再表示のシナリオを検証する
