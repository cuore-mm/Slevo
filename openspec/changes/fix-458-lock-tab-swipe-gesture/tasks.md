## 1. 入力方向ルーティング基盤の置換

- [x] 1.1 `BbsRouteScaffold` の本文モディファイアから `draggable` ベースの横抑止を撤去し、`pointerInput` ベースの方向分類入口を追加する
- [x] 1.2 ドラッグ開始方向を「横優勢・縦優勢・斜め」に分類し、同一ポインタ系列で分類結果を固定するロジックを実装する

## 2. 方向別のイベント消費制御

- [x] 2.1 横ドラッグ開始時は横ジェスチャー処理を維持しつつ、`HorizontalPager` にページ遷移イベントが伝播しないよう消費制御を実装する
- [ ] 2.2 縦ドラッグ開始時は本文リストとミニマップの縦スクロールを維持し、隣接タブ遷移を発生させない経路を確認する
- [x] 2.3 斜めドラッグ開始時は無効入力として扱い、本文スクロールとタブ切り替えの両方を確定しない制御を実装する

## 3. 受け入れ条件の検証

- [ ] 3.1 スレッド本文で縦スクロールを繰り返しても隣接タブへ誤遷移しないことを手動確認する
- [ ] 3.2 ミニマップの縦ドラッグ時に連続スクロールが機能し、隣接タブへ切り替わらないことを手動確認する
- [ ] 3.3 横ドラッグ開始時の既存ジェスチャー動作が維持されること、および斜め開始入力が無効化されることを手動確認する
