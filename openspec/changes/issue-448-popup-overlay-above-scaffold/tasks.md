## 1. ポップアップ描画責務の移設

- [ ] 1.1 `ReplyPopup` の描画位置を `ThreadScreen` content 内から Scaffold 上位レイヤーへ移設する
- [ ] 1.2 `ThreadScreen` と `ThreadScaffold` の責務を整理し、ポップアップ描画に必要な引数受け渡し経路を調整する
- [ ] 1.3 既存の重複入力遮断（content 側オーバーレイ等）を整理し、単一の外側タップ制御経路へ統合する

## 2. 挙動確認

- [ ] 2.1 ポップアップ表示中に bottomBar タップで最上位ポップアップが閉じることを確認する
- [ ] 2.2 content 外タップと bottomBar タップで同一の閉じる挙動になることを確認する
- [ ] 2.3 タップ投稿基準位置・段数別表示・既存アニメーションに回帰がないことを確認する
